# VERDANT ML backend
# (Windows) trust the system certificate store so HuggingFace downloads verify SSL
pip-system-certs>=4.0; platform_system == "Windows"
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.3
# ML — pretrained models, no training
torch>=2.2
transformers>=4.44
sentence-transformers>=3.0
httpx>=0.27
python-dotenv>=1.0
